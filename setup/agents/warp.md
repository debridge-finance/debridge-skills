
# Warp AI Terminal

1. Open Warp and go to Settings, AI, MCP. Click Add Server and enter name `debridge` with URL `https://agents.debridge.com/mcp`.

2. Load deBridge skills:

```shell
npx skill debridge-finance/debridge-skills
```

3. Save the configuration. Warp will connect to the deBridge server automatically.
